import React from "react";

import Checkbox from "../Checkbox";

describe("Checkbox", () => {
  test("Renders the Checkbox component", () => {
    // render(<Checkbox name="test" value="test" checked={true} />);
  });
});
