import React from "react";
import { render } from "@testing-library/react";

import Grn from "../Grn";

describe("Grn", () => {
    test("Renders the Grn component", () => {
        render(<Grn />);
    })
});